import { DevConfig } from 'src/types';

export const devConfig: DevConfig = {
  api: {
    driverApiUrl: 'https://staging.ukrainets.co.uk/', // TODO: change url to v1
    // driverApiUrl: 'https://jsonplaceholder.typicode.com/', // TODO: change url to v1
  },
};
